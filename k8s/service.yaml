apiVersion: v1
kind: Service
metadata:
  name: payroll-ui
  labels:
    app.kubernetes.io/name: payroll-ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payroll-ui
  ports:
    - name: http
      port: 80
      targetPort: http
